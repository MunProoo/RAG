# 프로젝트 에이전트 지침

## 기본 원칙

- 요청 범위 밖의 파일과 기존 사용자 변경은 수정하지 않는다.
- 작성하거나 수정하는 모든 함수에는 역할, 주요 입력 또는 주의점을 설명하는 주석이나 docstring을 둔다.
- 주석에 `-cursor` 문자열을 넣지 않는다.
- 구현 완료를 주장하기 전에 관련 테스트를 실행하고 실제 결과를 보고한다.
- 사용자가 요청하지 않은 커밋, 푸시, 데이터 삭제, DB 초기화는 하지 않는다.

## PL 개발 루프

사용자가 `pl` 에이전트를 선택하거나 PL 방식의 개발을 요청하면 다음 역할을 분리한다.

1. `pl`: 목표, 범위, 수용 기준, 검증 방법을 정의하고 전체 루프를 관리한다.
2. `developer`: 구현과 테스트를 수행한다.
3. `verifier`: 실제 diff와 테스트 결과를 독립적으로 확인한다.

검증 결과가 `REVISE`이면 PL은 구체적인 실패 근거와 수정 기준을 developer에 전달하고 다시 검증한다. 기본 반복 한도는 3회이며, 한도를 넘거나 외부 의존성 때문에 검증할 수 없으면 추측으로 합격 처리하지 말고 `BLOCKED`로 보고한다.

## 실패 학습 기록

- 모든 PL 루프는 작업 시작 전에 `PL_FAILURE_LOG.md`를 읽고 현재 작업에 적용할 예방 항목을 수용 기준에 포함한다.
- `REVISE` 또는 `BLOCKED`가 발생하면 PL은 재작업 전에 실패 원인, 증거, 해결책, 재발 방지 확인 방법을 `PL_FAILURE_LOG.md`에 기록한다.
- developer는 구현 전에 관련 실패 기록을 확인하고 예방 조치를 작업에 반영한다.
- verifier는 관련 예방 조치가 실제 코드와 테스트로 지켜졌는지 확인한다.
- 비밀값, 개인정보, 전체 로그 덤프는 기록하지 않는다. 같은 근본 원인의 실패는 새 항목을 중복 생성하지 말고 기존 항목에 재발 사실과 새 근거를 추가한다.

## RAG 변경 검증

- 검색 로직 변경 시 `rag/scripts/test_rag_regression.py`를 실행한다.
- 검색 품질 변경 시 가능하면 변경 전후 `rag/scripts/eval_retrieval.py` 결과를 비교한다.
- 청킹, 임베딩 모델, 메타데이터 규칙 변경은 재인덱싱 필요 여부를 명시한다.
- `chromadb` 버전은 pipelines와 indexer 사이에 동일하게 유지한다.
- 생성물인 `__pycache__`, `*.pyc`, 로컬 ChromaDB 데이터는 커밋 대상에서 제외한다.
